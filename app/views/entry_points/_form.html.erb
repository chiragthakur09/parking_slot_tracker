<%= form_with(model: entry_point, local: true) do |form| %>
  <% if entry_point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(entry_point.errors.count, "error") %> prohibited this entry_point from being saved:</h2>

      <ul>
        <% entry_point.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :point %>
    <%= form.number_field :point %>
  </div>

  <div class="field">
    <%= form.label :location_id %>
    <%= form.text_field :location_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
